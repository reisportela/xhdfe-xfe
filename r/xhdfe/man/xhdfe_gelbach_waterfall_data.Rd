% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gelbach_features.R
\name{xhdfe_gelbach_waterfall_data}
\alias{xhdfe_gelbach_waterfall_data}
\title{Gelbach waterfall data}
\usage{
xhdfe_gelbach_waterfall_data(
  x,
  focal = NULL,
  keep = NULL,
  drop = NULL,
  exact_match = FALSE,
  labels = NULL,
  include_other = TRUE,
  share_tol = 1e-12
)
}
\arguments{
\item{x}{An \code{xhdfe_gelbach} result.}

\item{focal}{Exactly one X1 coefficient.}

\item{keep, drop}{Regular expressions, or exact names when
\code{exact_match = TRUE}.}

\item{exact_match}{Use exact component matching.}

\item{labels}{Named display labels.}

\item{include_other}{Aggregate filtered components as
\code{Other (filtered)} to preserve the identity. Setting it to
\code{FALSE} deliberately leaves a filtered residual.}

\item{share_tol}{Movement denominator threshold.}
}
\value{
A list containing the waterfall rows and identity metadata.
  The final row contains \code{waterfall_residual}; it is approximately
  zero when \code{include_other = TRUE}.
}
\description{
Gelbach waterfall data
}
\details{
Selection happens on original component names before labels.
  The residual aggregate makes the visual path an accounting identity; it
  does not add causal or path-dependent interpretation.
}
\examples{
\dontrun{
specification <- xhdfe_gelbach_waterfall_data(
  fit, focal = "target", keep = "human",
  labels = c(human = "Human capital"), include_other = TRUE
)
}
}
