% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gelbach.R
\name{xhdfe_gelbach_contrast}
\alias{xhdfe_gelbach_contrast}
\title{Linear contrast of Gelbach components}
\usage{
xhdfe_gelbach_contrast(x, focal, groups, conf_level = 0.95)
}
\arguments{
\item{x}{An \code{xhdfe_gelbach} result.}
\item{focal}{One X1 name, one-based index, or logical mask selecting exactly
one X1 coefficient.}
\item{groups}{A character vector of group names with unit weights, or a named
numeric vector of arbitrary finite contrast weights. Names must match
\code{x$names}; duplicate and all-zero specifications are rejected.}
\item{conf_level}{Normal-approximation confidence level in (0,1).}
}
\value{
A one-row data frame containing \code{coefficient}, \code{estimate},
\code{std_error}, \code{conf_low}, \code{conf_high}, \code{conf_level}, and
\code{se_type}. A contrast containing any FE block is labelled
\code{joint_covariance_including_conditional_fe}, preserving the
conditional-FE inference qualification.
}
\description{
Computes a linear combination of named Gelbach components from the stored
joint covariance matrix. It does not re-estimate the model. Contrasts that
include absorbed-FE components retain their documented conditional-FE
inference label.
}
\seealso{\code{\link{xhdfe_gelbach}}, \code{\link{xhdfe_gelbach_tidy}}}
\examples{
\dontrun{
# Sum two components with unit weights
xhdfe_gelbach_contrast(
  fit, focal = "target", groups = c("human_capital", "job")
)

# Difference between two components
xhdfe_gelbach_contrast(
  fit, focal = "target", groups = c(human_capital = 1, job = -1)
)
}
}
