% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/groupfes.R
\name{xhdfe_group_fes}
\alias{xhdfe_group_fes}
\title{Decompose group/individual fixed effects}
\usage{
xhdfe_group_fes(
  y,
  X,
  fes,
  group,
  individual,
  weights = NULL,
  aggregation = "mean",
  weights_type = c("analytic", "frequency"),
  fit_intercept = TRUE,
  drop_singletons = TRUE,
  keep_singletons = NULL,
  tol = 1e-08,
  maxiter = 1e+05,
  tolerance_mode = "reghdfe-comparable",
  convergence = "auto",
  absorption_method = "auto",
  symmetric_sweep = FALSE,
  tol_main = 1e-09,
  tol_start = 0.001,
  tol_final = 1e-09,
  max_iter_main = 1e+05,
  max_iter_solver = 1000,
  verbose = 0,
  accel = 2,
  start_accel = 5,
  every_accel = 5,
  factor = 1,
  a1p1 = 0.75,
  a2p1 = 1e-08,
  a2p2 = 5,
  threads = 0
)
}
\arguments{
\item{y, X, fes, group, individual, weights, aggregation}{the model inputs, in
the matrix form of \code{\link{xhdfe_fit}}. \code{group} and
\code{individual} are required; \code{individual} must also appear in
\code{fes} (it is appended automatically by the high-level interface).}

\item{weights_type, fit_intercept, drop_singletons, keep_singletons, tol, maxiter, tolerance_mode, convergence, absorption_method, symmetric_sweep}{options for the internal grouped fit that precedes the decomposition;
pass the SAME values used for the model being decomposed so the
extractor conditions on the identical estimation state (defaults match
\code{\link{xhdfe_fit}}).}

\item{tol_main, tol_start, tol_final}{main-loop and solver tolerances
(defaults \code{1e-9}, \code{1e-3}, \code{1e-9}).}

\item{max_iter_main, max_iter_solver}{iteration caps (defaults
\code{100000} and \code{1000}).}

\item{verbose}{\code{0} for silent.}

\item{accel}{acceleration scheme: \code{0} none, \code{1} linear,
\code{2} geometric (default).}

\item{start_accel, every_accel}{when acceleration starts and how often it
is applied (defaults \code{5}, \code{5}).}

\item{factor}{tighten the solver tolerance when the MSE falls below
\code{factor * tol} (default \code{1}).}

\item{a1p1, a2p1, a2p2}{acceleration safeguards (defaults \code{0.75},
\code{1e-8}, \code{5}).}

\item{threads}{absorber thread count (0 = auto).}
}
\value{
An object of class \code{xhdfe_group_fes}: a list with
  \code{individual_ids}, \code{individual_effects}, \code{fe_level_ids},
  \code{fe_level_effects}, \code{iterations}, \code{converged} and
  \code{mse}.
}
\description{
Recovers individual-level and FE-level effect estimates for the
group-level-outcome estimator (the analogue of the Python module's
\code{extract_group_individual_fes}). It refits the grouped model
internally and then runs the iterative decomposition with the same
tuning knobs exposed by the Python binding.
}
\seealso{
\code{\link{xhdfe}} (arguments \code{group},
  \code{individual}, \code{aggregation})
}
